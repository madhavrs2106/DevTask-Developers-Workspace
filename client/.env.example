# Vite environment (client)
# Leave VITE_API_URL empty to use the built-in dev proxy (/api -> http://localhost:5000).
# For production builds, set the full API origin, e.g. https://api.yourdomain.com
VITE_API_URL=""
