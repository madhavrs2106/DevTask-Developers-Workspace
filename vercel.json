{
  "buildCommand": "npm run db:generate && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api?p=$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
